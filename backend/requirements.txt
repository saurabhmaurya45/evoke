# Runtime dependencies for the Vercel Python deployment (@vercel/python reads
# this file, not pyproject.toml). Keep in sync with [project.dependencies] in
# pyproject.toml — this list intentionally excludes uvicorn (Vercel's runtime
# calls the ASGI app directly, no server process needed) and alembic
# (migrations are an operator action, not something the request-serving
# function needs at runtime).
fastapi>=0.115
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy>=2.0.35
asyncpg>=0.30
pyjwt[crypto]>=2.9
httpx>=0.27
jsonschema>=4.23
bleach>=6.1
