# Wedding T6 — fixed static package

Root `index.html` with flattened CSS/JS, fixed broken HTTrack `.html` image links, local favicon/legacy assets, Firebase/RSVP runtime, and original video/audio references.

The supplied ZIP contains 404 HTML placeholders instead of the original wedding artwork/gallery media, so those assets remain linked to their original Firebase Storage URLs rather than being fabricated.
